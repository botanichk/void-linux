vchwd
Скрипт vchwd.sh.

Использование

```   ← открыть блок кода
nano vchwd.sh
```   ← закрыть блок кода

# вставьте сюда и введите содержание скрипта.

```   ← открыть блок кода
chmod +x vchwd.sh
```   ← закрыть блок кода

```   ← открыть блок кода
./vchwd.sh
```   ← закрыть блок кода
